<!doctype html>

<html>
<head>
	<title>Find a Gym Buddy</title>	
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link href="/css/bootstrap.min.css" rel="stylesheet">
	<link href="/css/bootstrap-theme.min.css" rel="stylesheet">
	<link href="/css/gitfit.css" rel="stylesheet">
   
</head>

<body>

	{{> _navbar}}

	<div class="container">
		<h1 position:"center">Find a Buddy</h1>

		<p>Check out the matches we found! Time to GitFit!</p>

 		{{#each buddies}}	
			<div class="random_matches">
				<div class="col-xs-4">
          <a href="profile/{{buddy.username}}" class="thumbnail">
						<img class="img" src="{{buddy.imageURL}}"></img>
					</a>
				</div>
        <div class="col-xs-8">
          <a href="profile/{{buddy.username}}">
              <h3 id="name">{{buddy.name.full}} had a match score of {{score}}!</h3>
          </a>
					<p>{{buddy.age}}</p>
					<p><i>{{buddy.location}}</i></p>
					<p>Gym: {{buddy.gym}}</p>
					<p>Profile: {{buddy.about_me}}</p>
				</div>
				<hr width="100%">
			</div>
		{{/each}}
		
	</div>

	<script src="https://code.jquery.com/jquery.js"></script>
	<script src="/js/bootstrap.min.js"></script>
</body>
</html>
